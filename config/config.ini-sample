# Template only.
# Real local values must be stored in config/config.ini.
# This sample file is the versioned example that can be committed to Git.

# Database connection example
db.default.driver = pgsql
db.default.server = localhost
db.default.port = 5432
db.default.user = your_db_user
db.default.password = your_db_password
db.default.database = your_db_name
db.default.schema = public
db.default.pfx =
db.default.encoding = UTF-8
db.default.serverTimeZone = UTC
db.default.logquery = false
db.default.debug = false

# Flyway
db.flyway.locations = classpath:db/migration
db.flyway.baseline-on-migrate = true
db.flyway.baseline-version = 1
db.flyway.baseline-description = Existing schema baseline
db.flyway.create-schemas = true

# First start
app.setup.enabled = true
app.registration.public = false

# Session
session.cookie.name = SESSION
session.cookie.secure = false
session.cookie.same-site = Lax
session.cookie.max-age = 2592000
session.datasource.pool-name = spring-users-session-pool
session.datasource.maximum-pool-size = 10
session.datasource.minimum-idle = 2
session.datasource.connection-timeout-ms = 30000
session.datasource.idle-timeout-ms = 600000
session.datasource.max-lifetime-ms = 1800000
session.datasource.keepalive-time-ms = 300000
session.datasource.validation-timeout-ms = 5000
session.datasource.initialization-fail-timeout-ms = 1
session.datasource.jdbc-params = tcpKeepAlive=true&connectTimeout=10&socketTimeout=30
session.cleanup.cron = 0 */10 * * * *
